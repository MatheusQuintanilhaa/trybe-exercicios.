const base = 5;
const height = 8;
const area = "resultado";
const perimeter = "soma";

resultado = base * height;
 console.log(resultado);

 soma = base + base + base + base + base;
 console.log(soma);
